; tweak the defaults (max_children 5, start_servers 2, min_spare 1, max_spare 3, max_requests 500
; you can override this by specifying a config filename that comes later in the alphabet
[www]
pm = dynamic
pm.max_children = 15
pm.start_servers = 5
pm.min_spare_servers = 5
pm.max_spare_servers = 10
